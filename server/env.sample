# Copy this to server/.env manually (file is gitignored)
# Update values as needed

# Server
PORT=4000

# MySQL connection URL for XAMPP (adjust user/password/db)
# Format: mysql://USER:PASSWORD@HOST:PORT/DATABASE
DATABASE_URL="mysql://root:password@localhost:3306/ecommerce_showcase"
